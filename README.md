# Tiktiger Foundation

This repository contains the Phase 3 Xcode Dynamic Library foundation only.

The target is isolated and contains no target-app integration, IPA packaging, runtime hooks, Substrate, Theos, Logos, or copied reference assets. Open `Tiktiger.xcodeproj` on macOS/Xcode for SDK and code-signing validation.
